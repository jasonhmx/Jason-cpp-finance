Description of the Binance book is in the file.

Notes on improvements:
Use `std::lower_bound` instead of implementing own binary search
During profiling, use `std::steady_clock` instead of high res clock
